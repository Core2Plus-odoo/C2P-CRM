# C2P-CRM

Samra CRM — Odoo integration work by [C2P Consultants](https://core2plus.com).

## Contents

| Path | What it is |
| --- | --- |
| [`demo/`](demo/) | Client-facing demo scripts. Currently the external-system integration demo, which proves live XML-RPC connectivity against a real Odoo instance. |

## Credentials

Nothing in this repository contains credentials, and nothing should. Copy
`.env.example` to `.env`, fill it in locally, and keep `.env` out of git —
`.gitignore` already covers it.
